#include "Server.hpp"

bool Server::handle_admin(std::string &rest) 
{
    std::cout << "In " << "admin" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_away(std::string &rest) 
{
    std::cout << "In " << "away" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_cnotice(std::string &rest) 
{
    std::cout << "In " << "cnotice" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_cprivmsg(std::string &rest) 
{
    std::cout << "In " << "cprivmsg" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_connect(std::string &rest) 
{
    std::cout << "In " << "connect" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_die(std::string &rest) 
{
    std::cout << "In " << "die" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_error(std::string &rest) 
{
    std::cout << "In " << "error" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_help(std::string &rest) 
{
    std::cout << "In " << "help" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_info(std::string &rest) 
{
    std::cout << "In " << "info" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_invite(std::string &rest) 
{
    std::cout << "In " << "invite" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_ison(std::string &rest) 
{
    std::cout << "In " << "ison" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_join(std::string &rest) 
{
    std::cout << "In " << "join" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_kick(std::string &rest) 
{
    std::cout << "In " << "kick" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_kill(std::string &rest) 
{
    std::cout << "In " << "kill" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_knock(std::string &rest) 
{
    std::cout << "In " << "knock" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_links(std::string &rest) 
{
    std::cout << "In " << "links" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_list(std::string &rest) 
{
    std::cout << "In " << "list" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_lusers(std::string &rest) 
{
    std::cout << "In " << "lusers" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_mode(std::string &rest) 
{
    std::cout << "In " << "mode" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_motd(std::string &rest) 
{
    std::cout << "In " << "motd" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_names(std::string &rest) 
{
    std::cout << "In " << "names" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_nick(std::string &rest) 
{
    std::cout << "In " << "nick" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_notice(std::string &rest) 
{
    std::cout << "In " << "notice" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_oper(std::string &rest) 
{
    std::cout << "In " << "oper" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_part(std::string &rest) 
{
    std::cout << "In " << "part" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_pass(std::string &rest) 
{
    std::cout << "In " << "pass" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_ping(std::string &rest) 
{
    std::cout << "In " << "ping" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_pong(std::string &rest) 
{
    std::cout << "In " << "pong" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_privmsg(std::string &rest) 
{
    std::cout << "In " << "privmsg" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_quit(std::string &rest) 
{
    std::cout << "In " << "quit" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_quote(std::string &rest) 
{
    std::cout << "In " << "quote" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_rehash(std::string &rest) 
{
    std::cout << "In " << "rehash" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_rules(std::string &rest) 
{
    std::cout << "In " << "rules" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_server(std::string &rest) 
{
    std::cout << "In " << "server" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_squery(std::string &rest) 
{
    std::cout << "In " << "squery" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_squit(std::string &rest) 
{
    std::cout << "In " << "squit" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_setname(std::string &rest) 
{
    std::cout << "In " << "setname" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_silence(std::string &rest) 
{
    std::cout << "In " << "silence" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_stats(std::string &rest) 
{
    std::cout << "In " << "stats" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_summon(std::string &rest) 
{
    std::cout << "In " << "summon" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_time(std::string &rest) 
{
    std::cout << "In " << "time" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_topic(std::string &rest) 
{
    std::cout << "In " << "topic" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_trace(std::string &rest) 
{
    std::cout << "In " << "trace" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_user(std::string &rest) 
{
    std::cout << "In " << "user" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_userhost(std::string &rest) 
{
    std::cout << "In " << "userhost" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_userip(std::string &rest) 
{
    std::cout << "In " << "userip" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_users(std::string &rest) 
{
    std::cout << "In " << "users" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_version(std::string &rest) 
{
    std::cout << "In " << "version" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_wallops(std::string &rest) 
{
    std::cout << "In " << "wallops" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_watch(std::string &rest) 
{
    std::cout << "In " << "watch" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_who(std::string &rest) 
{
    std::cout << "In " << "who" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_whois(std::string &rest) 
{
    std::cout << "In " << "whois" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_whowas(std::string &rest) 
{
    std::cout << "In " << "whowas" << " and this is the rest: " << rest << std::endl;
    return (true);
}
bool Server::handle_message(std::string &rest) 
{
    std::cout << "In " << "message" << " and this is the rest: " << rest << std::endl;
    return (true);
}